name: Deploy to spencers

on:
  workflow_dispatch:

concurrency:
  group: deploy-spencers
  cancel-in-progress: false

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: spencers
    secrets: inherit
