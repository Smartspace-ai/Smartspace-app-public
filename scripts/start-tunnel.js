/* eslint-disable no-console */
// Development tunnel placeholder: no tunnel is configured.
// Keep the process alive so that concurrently -k does not terminate the app task.
console.log('[TUNNEL] No tunnel configured. Running placeholder...');
setInterval(() => {
  // Keep alive; do nothing.
}, 60 * 60 * 1000);


