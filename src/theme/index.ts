/**
 * Theme entry. Use createAppTheme or appTheme for MUI; use tokens from @/theme/tokens in theme layer only.
 */

export { appTheme, createAppTheme } from './mui/createTheme';
export * from './tokens';
