// Public API surface of @smartspace/chat-ui.
//
// Consumers import three categories of things from this package:
//   1. The port: ChatService interface + ChatProvider + identity/context/service hooks.
//   2. UI components: MessageList, MessageComposer, etc.
//   3. Domain types/keys consumers need to integrate (Message, FileInfo, MessageThread,
//      Workspace, query keys for cache surgery if needed).
//
// Source code lives in src/. The public surface is everything re-exported from this
// barrel — anything not re-exported is package-internal.

// (Populated as files move into the package.)
export {};
