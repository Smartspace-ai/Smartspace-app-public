# pnpm config
# Hoist all deps to the root node_modules (matches npm layout). Keeps Nx,
# ESLint plugins, MUI, etc. working without explicit peer dep declarations.
shamefully-hoist=true
auto-install-peers=true
strict-peer-dependencies=false
