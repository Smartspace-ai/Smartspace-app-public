# pnpm config
# Pin every dep exactly (matches --save-exact used by the sdk-bump workflow
# and avoids SemVer surprises with our prerelease-tagged @smartspace/api-client).
save-exact=true
auto-install-peers=true
strict-peer-dependencies=false
